# requirements.txt 核心依赖（用户需要自行安装）

# ===== 后端 =====
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9          # 文件上传支持
pydantic>=2.6.0                  # 数据模型
pydantic-settings>=2.2.0         # .env 加载

# ===== Agent 框架 =====
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.7

# ===== LLM =====
openai>=1.30.0                   # OpenAI 兼容 SDK

# ===== 文档处理 =====
python-docx>=1.1.0               # DOCX 读写
lxml>=5.1.0                      # python-docx 底层依赖

# ===== 向量数据库 =====
chromadb>=0.5.0                  # 向量存储

# ===== 工具库 =====
python-dotenv>=1.0.1
loguru>=0.7.2                    # 或标准 logging (二选一，我们用标准logging)
jinja2>=3.1.3                    # HTML 报告模板渲染
aiofiles>=24.1.0                 # 异步文件操作
httpx>=0.27.0                    # 异步 HTTP

# ===== 测试 =====
pytest>=8.1.0
pytest-asyncio>=0.23.6
pytest-cov>=5.0.0